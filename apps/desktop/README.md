# AURA OS Desktop Shell Placeholder

This directory is reserved for the native desktop wrapper shell (e.g., Tauri / Rust) scheduled for Phase 8.

In Phase 1, AURA OS runs as an AI-native local FastAPI backend (`apps/shell/`) and React desktop UI (`apps/frontend/`).
